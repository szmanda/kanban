{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <h1>Assiging users to '{{ task.title }}'</h1>
    {{ form_start(form) }}

        {{ form_errors(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary">
            <i class="fa fa-save" aria-hidden="true"></i> {{ 'action.save'|trans }}
        </button>

    {{ form_end(form) }}
{% endblock %}